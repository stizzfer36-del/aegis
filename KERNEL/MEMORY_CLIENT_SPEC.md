# Memory Client Spec

The memory client abstracts session state, persistent memory, and skill promotion.

## Requirements

- query by trace, topic, and preference
- write only structured memory candidates
- support degraded fallback mode
- preserve provenance
